# مثال على ملف .env للإنتاج
# انسخ هذا الملف إلى .env وأدخل بيانات قاعدة البيانات الصحيحة

# PostgreSQL Database URL من Coolify
DATABASE_URL=postgresql://username:password@host:port/database_name

# أو يمكن استخدام DB_URL بدلاً من DATABASE_URL
# DB_URL=postgresql://username:password@host:port/database_name

# مثال على رابط PostgreSQL من Coolify:
# DATABASE_URL=postgresql://goldenhorse_user:your_password@postgres.coolify.io:5432/goldenhorse_db

# ملاحظات:
# - استبدل username بالمستخدم الصحيح
# - استبدل password بكلمة المرور الصحيحة  
# - استبدل host بعنوان الخادم الصحيح
# - استبدل port برقم المنفذ الصحيح (عادة 5432)
# - استبدل database_name باسم قاعدة البيانات الصحيح

# يمكنك الحصول على هذه المعلومات من:
# 1. لوحة تحكم Coolify
# 2. إعدادات قاعدة البيانات
# 3. تبويب "Environment Variables" أو "Database Info"
